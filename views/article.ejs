<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet"type="text/css" href="/styles/style.css">
  <link rel="icon" type="image/x-icon" href="/img/favicon.ico">
  <title><%= title  %></title>
</head>
<body>
  <main id="app">
    <article class="article" data-idx="<%= article.idx %>">
      <h2><%= article.title %></h2>
      <div class="article-mediaName"><%= article.mediaName %></div>
      <div class="article-summary"><%= article.summaryContent %></div>
      <div class="article-media">
        <a href="<%= article.url %>">Click here to read a full article.</a>
      </div>
      <footer>
        <div class="navigation">
          <a href="/">home</a><a href="/<%= category %>"><%= category %></a>
        </div>
        <div class="article-fav">
          <% if (article.isFav) { %>
            <img src="/img/star-full.svg" alt="Favourite">
          <% } else { %>
            <img src="/img/star-empty.svg" alt="Not favourite">
          <% } %>
        </div>
      </footer>
    </article>
  </main>
  <script type="module" src="/js/favorite.js" defer></script>
</body>
</html>